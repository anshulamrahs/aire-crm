export const selectSort = (state) => state.filter.sortOption;

export const selectFilters = (state) => state.filter.filterOption;

export const selectPage = (state) => state.filter.page;

export const selectLeadsCollection = (state) => state.filter.leadsCollection;

export const selectTotalLeads = (state) => state.filter.totalLeads;

export const selectTotalPages = (state) => state.filter.totalPages;

export const selectLimit = (state) => state.filter.limit;

export const selectLeadDetails = (state) => state.filter.leadDetails;